import { TypeSafeClient } from "@typesafe-ai/sdk";
export const typesafe=new TypeSafeClient({apiKey:process.env.TYPESAFE_API_KEY!});
export async function evaluateStudentAnswer(p:{question:string;idealAnswer:string;rubricCriteria:string[];studentAnswer:string}){const state=`Question: ${p.question}\nExpected Ideal Answer: ${p.idealAnswer}\nMandatory Criteria: ${p.rubricCriteria.join('; ')}\nStudent Answer: ${p.studentAnswer}`;const r:any=await (typesafe as any).systemOne({model:"jev-latest",state,questions:{is_correct:{type:"noul",prompt:"Does the answer satisfy the core criteria?"},score:{type:"score",prompt:"Rate accuracy and completeness.",min:0,max:1},error_category:{type:"choice",prompt:"Select the primary error.",options:{none:"Correct",misconception:"Flawed understanding",incomplete:"Missing essential steps",calculation_error:"Calculation mistake"}}}});return {isCorrect:r.answers.is_correct.value>.7,score:r.answers.score.value,errorType:r.answers.error_category.value,confidence:r.answers.is_correct.confidence};}
